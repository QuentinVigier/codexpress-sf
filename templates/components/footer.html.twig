<body class="flex flex-col min-h-screen">
	<header>
		<!-- Votre en-tête ici -->
	</header>

	<main
		class="flex-grow"><!-- Contenu principal de votre page ici -->
	</main>

	<footer class="py-8 mt-auto bg-violet-950 text-slate-50">
		<div class="flex flex-col items-center justify-between max-w-screen-lg px-4 mx-auto md:flex-row">
			<div class="mb-4 text-center md:mb-0 md:text-left">
				<p class="text-lg font-semibold">CodeXpress</p>
				<p class="text-sm">&copy;
					{{ 'now'|date('Y') }}
					- made by Quentin VIgier</p>
			</div>
			<div class="flex flex-col items-center md:items-end">
				<div class="flex mt-2 space-x-4">
					<a href="#" class="text-violet-50 hover:text-violet-200">
						{{ ux_icon('mdi:github', { 'class': 'h-6 w-6' }) }}
					</a>
					<a href="#" class="text-violet-50 hover:text-violet-200">
						{{ ux_icon('mdi:twitter', { 'class': 'h-6 w-6' }) }}
					</a>
					<a href="#" class="text-violet-50 hover:text-violet-200">
						{{ ux_icon('mdi:linkedin', { 'class': 'h-6 w-6' }) }}
					</a>
				</div>
			</div>
		</div>
	</footer>
</body>
