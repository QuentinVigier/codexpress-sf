<nav class="flex items-center justify-between p-2 bg-slate-50">
	<a href="{{ path('app_home') }}" class="flex items-center gap-1">
		<img src="{{ asset('/img/logo_dark.svg') }}" alt="logo" class="transition-transform duration-200 ease-in-out rounded-full hover:scale-95">
		<p>CodeXpress</p>
	</a>
	<div class="flex items-center justify-between gap-4">
		<form action="" method="post" class="items-center hidden p-1 rounded-md md:flex bg-slate-100">
			<input type="hidden" name="csrf_token" value="{{ csrf_token('authenticate') }}">
			<input type="text" name="" id="" placeholder="Search notes..." class="bg-transparent outline-none">
			<button type="submit">{{ ux_icon('mdi:search', { 'class': 'h-6 w-6 text-neutral-600 hover:text-violet-600' }) }}</button>
		</form>
        <div class="flex items-center justify-between gap-4">
            <a href="#" class="p-2 rounded-md bg-slate-200 md:hidden">
				{{ ux_icon('mdi:search', { 'class': 'h-6 w-6 text-neutral-600 hover:text-violet-600' }) }}
			</a>
            <a href="{{ path('app_note_new')}}" class="p-2 rounded-md bg-slate-200">
				{{ ux_icon('mdi:plus-box-outline', { 'class': 'h-6 w-6 text-neutral-600 hover:text-violet-600' }) }}
			</a>
            <a href="{{ path('app_profile_edit')}}" class="p-2 rounded-md bg-slate-200">
				{{ ux_icon('mdi:cog-outline', { 'class': 'h-6 w-6 text-neutral-600 hover:text-violet-600' }) }}
			</a>
            <a href="{{ path('app_note_user', { 'username': app.user.username ?? 'martin' })}}" class="transition-transform duration-200 ease-in-out rounded-full shadow-md hover:scale-95">
				<img src="https://avatar.iran.liara.run/public/50" alt="logo" width="48" class="rounded-full">
			</a>
			
			{% if app.user is not null %}
			<a href="{{ path('app_logout') }}" class="p-2 rounded-md bg-slate-200">
				{{ ux_icon('mdi:logout', { 'class': 'h-6 w-6 text-neutral-600 hover:text-violet-600' }) }}
			</a>
			{% endif %}
        </div>
	</div>
</nav>
