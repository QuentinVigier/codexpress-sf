{% extends 'base.html.twig' %}

{% block title %}
	Add a new note
{% endblock %}

{% block body %}
	{# {% if app.user is defined %}
		{{ include('components/denied.html.twig') }} 
        Access denied
	{% else %}
		{{ form(noteForm) }}{% endif %} #}
        {{ form(noteForm) }}
	{% endblock %}
